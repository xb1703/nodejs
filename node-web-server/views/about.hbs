<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>About</title>
  </head>
  <body>
    <h1>{{upperCase title}}</h1>
    <p><img src="img/{{image}}" /></p>
    <p><ul>
      {{#each users}}
      <li>
        {{name}}
      </li>
      {{/each}}
    </ul>
    </p>
  </body>
  <!-- inclusion du footer dans partials-->
  {{>footer}}
</html>
